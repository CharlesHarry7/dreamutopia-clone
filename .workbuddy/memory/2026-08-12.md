# 2026-08-12 工作日志

## taletok.io 1:1 克隆项目 (Cloudflare 全栈)

### 背景
- 从 columbus.tools AI ranking (video-production 分类) 20 个网站中选定了 taletok.io 作为克隆目标
- 用户选择 Cloudflare 全栈替代 ShipFast (Vercel) 方案以降低成本 ($0/月 vs $20+/月)

### 完成项
1. **静态前端** — `out/index.html` (494行, 48KB)
   - 纯 HTML + Tailwind CDN + 内联 CSS + vanilla JS
   - 12 个区块: Navbar, Hero, LongFormEpisodes, HowItWorks, TrendingFormats, FormatShowcase, GlobalReach, FeaturedOn, Monetisation, Pricing, FAQ, Footer
   - 零构建步骤，直接部署到 Cloudflare Pages
   - 因 npm install 3次 OOM (exit 137)，从 Next.js 方案降级为纯静态 HTML

2. **Cloudflare 后端文件** (已创建但 npm install 未成功，未测试)
   - `wrangler.toml` — D1/KV/R2 bindings 配置
   - `schema.sql` — users/subscriptions/sessions 三表
   - `libs/` — env.ts, db.ts, auth.ts (JWT+KV), stripe.ts (fetch API), email.ts (Resend)
   - `functions/api/` — health, auth/login, auth/logout, auth/me, stripe/checkout, stripe/webhook
   - `package.json` — @cloudflare/workers-types, wrangler

### 部署方式
- 静态版: `out/` 目录直接上传 Cloudflare Pages，零构建
- 全栈版: 需先解决 npm install OOM 问题，然后 `npm run build && wrangler pages deploy`

### 待办
- [ ] 浏览器端视觉验证 1:1 还原度
- [ ] 解决 npm install OOM (考虑增加 swap 或分批安装)
- [ ] 后端 API 联调测试
- [ ] Cloudflare Pages 实际部署

## DreamUtopia 1:1 临摹项目 (替代 taletok-clone)

### 决策
- 用户在 columbus.tools 视频生产分类里要求"找最好的" — 对比后选定 **dreamutopia.net** (图生视频 AI 平台)
- 理由: DR 26 最高 + 3月增长 2633% + 页面结构最完整 (生成器工作台/对比表/作品墙/FAQ) + 无 NSFW 风险
- 用户明确要求 1:1 临摹，不要重新设计 (之前自作主张写"抖视AI"被否)

### 完成项
1. **GitHub 仓库改名** — `CharlesHarry7/taletok-clone` → `CharlesHarry7/dreamutopia-clone`
2. **代码推送** — `out/index.html` (27.8KB) 1:1 临摹 DreamUtopia 首页
   - 完整结构: 促销条/Header/Video-Image tabs 生成器/Hero/3功能卡/Free vs Credits 对比/12宫格作品墙/Why/3步流程/Credits CTA/App banner/8 FAQ/Footer
   - 已剔除原站 footer NSFW 链接
   - commit: 265e254
3. **推送方式** — git push 被 GFW 拦 (HTTP2 framing error)，走 github-api-push skill 的 Git Data API (base_tree 增量模式)

### 环境记录
- GH_TOKEN: `security find-internet-password -s github.com -a guochao950518-wq -w`
- 远端 main 基线: 02b572f
- Cloudflare Pages 项目 `taletok-clone` 已存在但此前上传后访问 404 (待处理)
